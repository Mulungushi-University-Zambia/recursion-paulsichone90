START 
  FUNCTION FIBONACCI(N)
    ENTER N
    IF N == 0 THEN, 
       RETURN 0 
    ELSE IF N == 1 THEN,
      RETURN 1
    ELSE
     RETURN FIB(N-1) + FIB(N-2)
     SET RESULT = FIBONACCI(N)
     DISPLAY RESULT
  END FUNCTION
STOP